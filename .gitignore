# build intermediates
build/
.gradle/
app/build/

# locally built artifacts - the released APKs/AAB come from CI (see README)
dist/
screenshots/

# ide / os
.idea/
*.iml
.DS_Store
local.properties

# real signing material for your own releases (the committed keystore/ is a labeled demo key)
*.production.jks
*.production.keystore
